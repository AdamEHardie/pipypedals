setup venv for the project:
`python -m venv venv`

activate the venv:
`source venv/bin/activate`

install the dependencies/pip packages for the project:
`pip install -r requirements.txt`

run the app (be sure to have activated venv and installed packages):
`python main.py`

